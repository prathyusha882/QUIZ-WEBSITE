DEBUG=True
SECRET_KEY=super-secret-key
DB_NAME=quizdb
DB_USER=quizuser
DB_PASSWORD=quizpass
DB_HOST=db
DB_PORT=5432
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=django-db
ALLOWED_HOSTS=localhost,127.0.0.1

# Email settings for password reset and verification (update with your actual email SMTP credentials)
DEFAULT_FROM_EMAIL=no-reply@studentquizplatform.com
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password

# Frontend URL for password reset and email verification links
FRONTEND_URL=http://localhost:3000

# JWT token lifetimes (minutes / days)
ACCESS_TOKEN_LIFETIME_MINUTES=15
REFRESH_TOKEN_LIFETIME_DAYS=7
